import teamsTemplate from 'text!./teams.html';
import TeamsController from './teams.controller';

const TeamsComponent = {
  template: teamsTemplate,
  controller: TeamsController
};

export default TeamsComponent;
